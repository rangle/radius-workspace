#!/usr/bin/env node

"use strict";

const location =
  process.env["RADIUS_MODE"] === "development" ? "../lib/cli" : "../dist/cli";
const cli = require(location);
const parser = cli();

// eslint-disable-next-line no-unused-expressions
parser.parse(process.argv.slice(2));
